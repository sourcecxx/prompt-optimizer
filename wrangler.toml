name = "prompt-optimizer"
compatibility_date = "2026-05-01"
main = "./packages/web/dist/index.html"

[site]
bucket = "./packages/web/dist"

# 构建配置（Cloudflare Pages 使用 build.command）
[build]
command = "pnpm build"
